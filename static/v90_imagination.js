(()=>{const q=s=>document.querySelector(s),money=n=>Number(n||0).toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2});function wait(){const lab=q('[data-view-panel="ai"]');if(!lab)return setTimeout(wait,140);if(q('#aiImaginationCard'))return;const c=document.createElement('section');c.id='aiImaginationCard';c.className='card ai-imagination-card';c.innerHTML=`<div class="section-title"><div><span class="eyebrow">FORWARD SIMULATION / IMAGINATION • V9.0</span><h2>Think through several fake futures before committing</h2></div><span class="safe-pill">SIMULATOR ONLY</span></div><p class="ai-imagination-intro">Purple now stress-tests a candidate through several tiny synthetic futures before final fake-money sizing. The scenarios do not claim to predict the market; they are a lightweight way to test whether a setup survives upside, failure, volatility, broad reversal, correlation stress, and sideways conditions.</p><div class="ai-imagination-controls"><input id="aiImagineSymbol" value="NVDA" maxlength="24" aria-label="Symbol"><button id="aiImagineRun" class="secondary-button">RUN IMAGINATION</button></div><div id="aiImagineHero" class="ai-imagination-hero"><div><span>Status</span><b>Waiting</b><small>Pick a symbol and simulate.</small></div></div><div id="aiScenarioGrid" class="ai-scenarios"></div><div id="aiImagineSummary" class="ai-imagination-summary">The final multiplier can shrink or block a simulated allocation before Autopilot places a fake-money buy.</div>`;lab.appendChild(c);q('#aiImagineRun').onclick=run;document.addEventListener('click',e=>{const row=e.target.closest?.('.ai-candidate');if(!row)return;const s=row.querySelector('.ai-symbol b')?.textContent;if(s){q('#aiImagineSymbol').value=s;run()}})}async function run(){const b=q('#aiImagineRun'),sym=(q('#aiImagineSymbol').value||'NVDA').trim().toUpperCase();if(!sym)return;b.disabled=true;b.textContent='SIMULATING…';q('#aiImagineHero').innerHTML='<div><span>Imagination</span><b>Building futures…</b><small>Running lightweight stress paths.</small></div>';try{const d=await api('/api/ai/imagine',{method:'POST',body:JSON.stringify({symbol:sym})}),i=d.imagination||{};q('#aiImagineHero').innerHTML=`<div><span>${d.candidate?.symbol||sym}</span><b>${i.permission||'—'}</b><small>final target ${Number(d.final_target_pct||0).toFixed(2)}%</small></div><div><span>Robustness</span><b>${Number(i.robustness_score||0).toFixed(1)}</b><small>0–100</small></div><div><span>Imagination multiplier</span><b>×${Number(i.imagination_multiplier||0).toFixed(2)}</b><small>after hierarchical routing</small></div><div><span>Positive scenario weight</span><b>${Number(i.positive_scenario_probability||0).toFixed(1)}%</b><small>synthetic paths</small></div><div><span>Worst account impact</span><b>-${Number(i.worst_account_impact_pct||0).toFixed(2)}%</b><small>at pre-imagination size</small></div>`;q('#aiScenarioGrid').innerHTML=(i.scenarios||[]).map(s=>`<article class="ai-scenario ${s.kind}"><div class="ai-scenario-head"><b>${s.label}</b><strong>${Number(s.simulated_move_pct).toFixed(2)}%</strong></div><div class="ai-scenario-meta"><span>Weight<b>${Number(s.probability).toFixed(1)}%</b></span><span>P/L<b>${money(s.position_pl)}</b></span><span>Equity<b>${money(s.projected_equity)}</b></span></div></article>`).join('');q('#aiImagineSummary').textContent=`Weighted synthetic return ${Number(i.weighted_scenario_return_pct||0).toFixed(2)}%. ${i.reasons?.length?i.reasons.join(' • '):'No extra imagination gate triggered.'} These are simulator stress cases, not forecasts.`}catch(e){q('#aiImagineHero').innerHTML=`<div><span>Error</span><b>Imagination unavailable</b><small>${e.message}</small></div>`}finally{b.disabled=false;b.textContent='RUN IMAGINATION'}}wait();})();